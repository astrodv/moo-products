import React from "react"

interface Props {
    children: React.ReactNode,
    size?: number,
    paddingBottom?: number,
    paddingTop?: number,
    marginBottom?: number,
    marginTop?: number,
}

export default Props
